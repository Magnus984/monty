#include "monty.h"

/**
 * nop - doesn't do anything
 * @stack: pointer to stack
 * @line_number: line where opcode is found
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
